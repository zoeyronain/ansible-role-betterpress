---

  tasks:
    - name: get databases
      community.mysql.mysql_info:
          filter: databases
          return_empty_dbs: yes
      register: dbStatus
    - name: create database if not exists
      mysql_db:
          name: zoeyrae
          state: present
